function count() {

    var animals = ["Lion", "Elephant", "Dinosaur", "Monkey"];

    for (var i = 0; i < animals.length; i++) {
        window.alert("This zoo contains a " + animals[i] + ".");
    }
}